name: Terraform
run-name: Terraform-test

on:
  workflow_dispatch:
    inputs:
      env:
        description: Environment
        default: dev
        required: true
jobs:
  build:
    name: checkout
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout

      - name: Test Command
        run: echo "Hi"
